PRN Rename Note

- Created `prn-file.xml` as a copy of `pom.xml` upon request.
- Windows reserves the `PRN` device name, so naming a file `prn.xml` is not allowed.
- Note: Maven expects `pom.xml`. Renaming the Maven project file will break standard Maven commands unless you explicitly reference the alternative filename.

If you want me to also replace references or update documentation to use `prn-file.xml`, tell me which references to update.